# Secrets — NIKDY necommituj
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Service account kľúče a akékoľvek lokálne JSON — TAJNÉ, nikdy do gitu
# (projekt nemá žiadny .json, ktorý by mal byť verzovaný)
*.json

# Dáta klientov (objednávky, exporty) — môžu obsahovať osobné údaje, nikdy do gitu
*.csv
data/

# Databázy (konverzácie + objednávky z CMS — orders.db obsahuje dáta klienta)
*.db

# Logy bežiacich procesov
*.log
*.log.old

# OS
.DS_Store
